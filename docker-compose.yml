services:
  caddy:
    image: caddy:2.10-alpine
    restart: unless-stopped
    env_file:
      - .env
    network_mode: host
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_data:
  caddy_config:
